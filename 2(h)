//Calculate 1-x/2+x^(2)/4-x^(3)/6
#include <stdio.h>
#include <math.h>
int main() {
float sum=0,x,y;
int i;
printf("Enter x\n");
scanf("%f", &x);
for(i=0;i<=3;i++){
    y=pow*((-x/2),i);
    sum=sum+y;
}
 printf("Sum is %f\n", sum);
return 0;
}
